extends layout 

block content
  form(action="/new", method="post") 
    h1 Add message
    p name: 
      input(type="text" name="name")
    p message: 
      input(type="text" name="message")
    input(type="submit") 